{
  "name": "sports-orca-nba-matches",
  "version": "1.0.0",
  "description": "NBA Upcoming Matches Display - SportsOrca Internship Task",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-deps": "npm install && cd server && npm install && cd ../client && npm install",
    "heroku-postbuild": "npm run install-deps && npm run build",
    "lint": "eslint ."
  },
  "keywords": ["nba", "sports", "react", "node", "api"],
  "author": "SportsOrca Intern",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
